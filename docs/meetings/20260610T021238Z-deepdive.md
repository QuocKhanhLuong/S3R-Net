# AgenTeam Deepdive: S3R/SSR Block Audit

Date: 2026-06-10 09:12 Asia/Ho_Chi_Minh

## Health: NO ACTIVE RUN

ATeam runtime returned `health=no-active-run` and output path
`docs/meetings/20260610T021238Z-deepdive.md`.

## External Signals (Researcher)

- KD and teacher-gate experiments are out of scope for this pass.
- The relevant audit target is the original S3R/SSR spectral residual block and
  whether spectral components actually affect segmentation metrics.

## Internal Health (Architect)

- `SSRFullBlock` already implemented FFT band split, retain/update/suppress
  gates, high-frequency ratio penalty, bounded gamma, and block logs.
- Existing logs were not enough to answer whether the residual branch matters:
  missing explicit residual norm, gamma raw/effective, output delta, FFT
  reconstruction error, and complete per-band energy diagnostics.
- Variants should be implemented inside the original block, not through KD or
  teacher modules.

## Recommendations (PM/QA)

1. **Add block variants**
   Priority: high
   Effort: medium

   Implement `s3r_full`, `s3r_gamma0`, `s3r_fft_identity`, `s3r_fixed_band`,
   `s3r_no_suppress`, and `s3r_simple_spectral`.

2. **Log per-block evidence**
   Priority: high
   Effort: medium

   Save `s3r_block_logs.csv` and W&B block metrics so gamma, spectral energy,
   gates, residual ratio, and high-frequency behavior can be inspected per
   block/stage.

3. **Add FFT and variant tests**
   Priority: high
   Effort: small

   Cover radial mask reconstruction, gamma-zero identity, FFT identity, and
   finite outputs for every block variant.

4. **Run controlled no-KD experiments**
   Priority: high
   Effort: large

   Run all variants with the same ACDC 25D, 224, batch 8 setup as the best
   supervised baseline.

## Action Items

- Dev: implement block variants and diagnostics in `SSRFullBlock`.
- Dev: expose `--block_variant` in the trainer.
- QA: run `pytest tests/test_s3r_core.py -q` in the server `specmamba`
  environment.
- PM: compare the six 50-epoch summaries before deciding which two variants
  deserve 200/250 epochs.
